<Window x:Class="AppieApplication.ShoppingListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ShoppingList" Height="255" Width="570"
        DataContext="{Binding ShoppingListViewModel, Source={StaticResource Locator}}">

    <Grid>

        <DataGrid AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding SvmList}" Margin="5" HorizontalAlignment="Center" VerticalAlignment="Top">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding ProductName}"/>
                <DataGridTextColumn Header="Number" Binding="{Binding Count}"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock VerticalAlignment="Bottom" HorizontalAlignment="Right" Text="{Binding TotalPrice}"/>
        
    </Grid>

</Window>